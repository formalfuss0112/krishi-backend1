services:
  - type: web
    name: krishi-backend
    runtime: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: FLASK_ENV
        value: development
    plan: free
